node_modules/
dist/
dist-ssr/
*.local

.env
.env.local
.env.*.local
.env.local.save
# Editor backup variants (nano `.save`, vim `.swp/~`, emacs `#`)
*.save
*.swp
*~
\#*\#

.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript incremental build cache — local-only
*.tsbuildinfo

# Vercel build artifacts (will only appear if running `vercel` locally)
.vercel/
